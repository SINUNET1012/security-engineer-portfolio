변경 공통사항
return 시에 cmd1, cmd2 이렇게 주지않고 그냥 하나의 cmd만 넘겨주도록 했음.

############ SMB SERVER ###########

1. smb id 생성 및 passwd 설정 분리
passwd부분 자동화함.


2. 계정정보 출력(계정이 이 시스템에 존재하는지, 해당 계정의 uid 셸 설정 등을 확인)
#     def smb_id_password_info(self):
#         cmd = f"cat /etc/passwd | grep {self.smb_id}"
#         return cmd
불필요해서 제거

3. 함수 위치변경 (실행 순서에 따라서..)
   #smb 계정 확인 명령어
    def id_check():
        cmd = "pdbedit —list"
        return cmd

4.
	[share]

                comment = {self.smb_id}'s SMB
                hosts allow = 172.16.0.0/16
                path = {self.smb_share_folder}
                read only = No
                valid users = {self.smb_id}
                write list = self.smb_id}
        EOF
        """

(valid users : 이 공유 폴더에 접속 자체가 허용된 사용자
-> 반드시 smb_id만 접근하게 할 이유가 있는지
write list : 파일 생성 / 수정 / 삭제가 가능한 사용자 
-> 이거하면 writeable 안적어도됨)

hosts allow 삭제 서버 구현시 ip가 172대역이 아닐 수 있음 10.대역일 수도있음
좋은 아이디어지만, 어처피 같은 대역에서 작성할 가능성이 크고
만약 사용할거면 input으로 값을 받는게 나음

[share] -> 이 부분을 share block 공유폴더라고 하는데
client에서는 이 공유폴더를 입력을 받고 있으므로 서버에서도 share를 입력받을 수 있게함
입력 받는편이 더 좋은이유는, 공유폴더를 다양하게해야함
또한 여러개의 smb 계정을 만들고 다른 smb 폴더를 만들때에
문제가 발생
물론 이렇게 share를 변경안하고 진행하는 방법이 있는데
그럴시에는 계정관리를 해줘야함.
근데 그 부분이 누락되어있으므로 share block을 추가했음.

############ SMB CLIENT ###########
함수 이름 변경. 및 추가 
(로그인과 마운트를 분리함)

지역변수를 쓰고싶어 하신것 같지만은...
두 함수로 나누고 그 함수가 비슷한 변수를 많이 써서 self로 줬습니다.

+ 로그인하는 코드 없어서 expect로 코드 추가했습니다.
